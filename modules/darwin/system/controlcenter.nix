{ ... }:

{
  # Control Center > menu-bar items (from docs/mac_reminders.md)
  system.defaults.controlcenter = {
    BatteryShowPercentage = true;
    Sound = false; # hide from menu bar
    NowPlaying = false;
  };
}
